export declare class DateHelper {
    parseSelections(format: string): any[];
    parseDate(value: string, format: string): Date;
    formatDate(date: Date, format: string): string;
}
export declare const dateHelper: DateHelper;
